spring:
  application:
    name: claude-invoice-tool
  output:
    ansi:
      enabled: ALWAYS
  ai:
    anthropic:
      api-key: ${ANTHROPIC_API_KEY}
      chat:
        options:
          model: claude-haiku-4.5
          temperature: 0.3
          max-tokens: 300

server:
  port: 9000